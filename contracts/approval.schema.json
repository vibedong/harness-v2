{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "harness-v2://contracts/approval.schema.json",
  "title": "HARNESS V2 Approval Scope",
  "description": "Approval scope is an evidence carrier, not authority generator. Folders, registry rows, log rows, review notes, route rows, release notes, package metadata, test pass output, and agent claims do not create or widen approval.",
  "type": "object",
  "required": ["packet", "approved_paths"],
  "properties": {
    "packet": { "type": "string", "minLength": 1 },
    "approved_paths": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "excluded_side_effects": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    }
  },
  "additionalProperties": true
}
